public class VaultKeepKeepKeep {
  public VaultKeepKeepKeep(VaultKeep data)
  {
    string CreatorId = data.Keep.CreatorId;
    Profile Creator = data.Keep.Creator;
    int Id = data.Keep.Id;
    DateTime CreatedAt = data.Keep.CreatedAt;
    DateTime UpdatedAt = data.Keep.UpdatedAt;
    string Name = data.Keep.Name;
    string Description = data.Keep.Description;
    string Img = data.Keep.Img;
    int Views = data.Keep.Views;
    int Kept = data.Keep.Kept;
    int VaultKeepId = data.VaultKeepId;
  }
}